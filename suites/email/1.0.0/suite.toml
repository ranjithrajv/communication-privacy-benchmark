schema_version = "1alpha1"
suite_id = "email"
version = "1.0.0"
status = "draft"
title = "Email remote-content pilot suite"
description = "The first canonical wedge: two native mail clients reading the same synthetic Gmail account from the same vantage, so a difference between rows is attributable to the client. Draft because the EPT gateway is not pinned or deployed, no synthetic mailbox is provisioned, the physical Mac lane does not exist, and the Google provider-terms review is pending."
checks = ["email.remote-content@1.0.0"]
subjects = [
  "apple-mail-gmail-consumer@1.0.0",
  "thunderbird-gmail-consumer@1.0.0",
]
repetitions = 3
